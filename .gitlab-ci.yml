docs:
  stage: build
  tags: [nix]

  script:
    - ./shake docs
    - ./ci-deploy

check:
  stage: build
  tags: [nix]
    
  script:
    - ./shake check
    - ./shake test

# TODO artefact is too big for current CI limits
# vscode:
#   stage: build
#   tags: [nix]

#   script:
#     - ./geolog-lsp/scripts/package.sh
#   artifacts:
#     paths:
#       - geolog-lsp/client/geolog-lsp-client*.vsix
